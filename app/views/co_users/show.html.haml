%p#notice= notice
= link_to account_management_index_url, class: "waves-effect waves-light link_back" do
  %i.material-icons keyboard_arrow_left
  Back to List Co-Users

%h1 Edit co-user's rules

.switch
  %label 
    Edit
    %input#editable{:type => "checkbox"}/
    %span.lever

=render 'rules/edit_form'

%div
%h3 Outgoing transactions
-if @transactions
  %table
    %thead
      %tr
        %th To:
        %th Summ:
        %th Created:
    %tbody
    - @transactions.each do |t|
      %tr
        %td= t.remote_account_id
        %td= t.summ
        %td= t.created_at.to_formatted_s(:short)
%h3 Unconfirmed transactions
-if @unconfirmed
  %table
    %thead
      %tr
        %th To:
        %td Summ:
        %td From_id:
        %td Created:
    %tbody
    - @unconfirmed.each do |t|
      %tr
        %td= t.remote_account_id
        %td= t.summ
        %td= t.account_id
        %td= t.created_at.to_formatted_s(:short)
        %td= link_to 'Confirm', account_transaction_path(@account, t), :method => :put, :class => 'btn btn-sm btn-danger'

