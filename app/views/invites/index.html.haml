=notice

=form_for :invite do |f|
  =f.label 'Enter co-user email'
  =f.email_field :email, placeholder: 'Enter email'
  =f.label 'Enter limit'
  =f.number_field 'rule[spending_limit]', within: 1...999
  =f.submit 'Send invite'

%ul
  -@invites.each do |invite|
    %li
      User name:
      =User.find(invite.user_to_id).name
      Invited at:
      =invite.created_at
      =link_to 'Delete invite', account_invite_path(invite.account_id, invite), method: :delete, data: { confirm: 'Are you sure?' }
