%h3 Transactions
-if @transactions
  %table
    %thead
      %tr
        %th User:
        %th From:
        %th To:
        %th Summ:
        %th Created:
      %tbody
      - @transactions.each do |t|
        %tr
          %td= t.user.email
          %td= t.account.iban
          %td= t.remote_account_id
          %td= t.summ
          %td= t.created_at.to_formatted_s(:short)